# SYNTHETIC BASELINE — NOT COMPILE OUTPUT

The credit-trading v1.0 skill was authored directly, not produced by the
compile pipeline. This file exists to make that fact impossible to miss.
Every future version (v1.1+) will be produced by the real pipeline and
will have normal provenance metadata. Until a real v1.1 exists, treat
v1.0's "provenance" as documentation of the seed, not derivation.

---

## Metadata

```yaml
skill: credit-trading
version: v1.0
status: frozen-baseline
backfill_pending: true
created: 2026-04-05
notes: "Baseline inherited from v1 work before v2 restructure. Provenance graph is unknown — this version was compiled from conversations and sources that predate the new training corpus structure."
```

## What v1.0 contains

This version was imported from v1 work. It contains methodology developed through prior conversations, practitioner primers (JunkBondInvestor, Moody's credit handbook, sell-side primers), framework sources (Howard Marks memos, Klarman, Fridson, Altman, Moyer), and direct distillations from Claude conversations. The specific mapping between sources and sections of the compiled skill is not documented.

## Backfill procedure

Run a backfill for the v1.0 baseline skill only when:
- v2 has been operational for 4+ weeks
- At least one native-provenance version (v2.0+) has been compiled and published
- You want historical audit capability for decisions that were shaped by v1.0

Backfill is never urgent. The system works without it.

### Steps

1. Open `training/_compiled/credit-trading/v1.0/SKILL.md`
2. For each section, identify candidate source material from: your memory, prior Claude conversations, primers/books you've read, framework sources you've drawn on
3. For each identified source, add a retroactive entry to this file with frontmatter `backfilled: true, backfilled_date: <today>, confidence: low|medium|high`
4. Work incrementally. A partial backfill is valuable. Don't wait until you can do the whole thing.
5. Commit each backfill step with message `v2-provenance-backfill: v1.0 section <name>`

### What not to do

- Do NOT modify the v1.0 skill itself during backfill. v1.0 is frozen.
- Do NOT mark backfilled entries as if they were native. The honesty of the backfill flag is what makes this useful.
- Do NOT skip writing `confidence: low` when that's the truth. Low-confidence backfill is better than no backfill, but it must be labeled.

## Next version

v2.0 and onward will have full native provenance. The first recompile after sufficient training content accumulates (expected: 5+ primers, 5+ frameworks, 2+ post-mortems) will produce v2.0 with a complete provenance graph.
